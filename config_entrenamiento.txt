# Configuración de entrenamiento del Random Forest
# Edita los valores y corre:           python main.py -rf
# Restaurar estos valores de referencia: python main.py --reset-config
# (Los flags de CLI como --n-estimators sobreescriben lo de aquí.)

# nº de árboles del bosque
n_estimators = 300

# profundidad máxima (None = sin límite)
max_depth = None

# mínimo de muestras por hoja
min_samples_leaf = 1

# mínimo de muestras para dividir un nodo
min_samples_split = 2

# features por split: sqrt | log2 | entero | fracción
max_features = sqrt

# criterio de división: gini | entropy | log_loss
criterion = gini

# muestreo con reemplazo: true | false
bootstrap = true

# desbalance: balanced | none
class_weight = balanced

# semilla del modelo (el split ya está fijo)
seed = 42

# guardar un checkpoint cada N árboles
checkpoint_cada = 50

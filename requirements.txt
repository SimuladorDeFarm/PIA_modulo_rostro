# Núcleo del pipeline de QC (tareas 1-5 y 7-9)
Pillow>=12.0
pandas>=2.0
numpy>=1.24

# Entrenamiento del Random Forest (clasificador de emociones)
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.7      # gráficos: matriz de confusión y métricas

# Solo para correr Py-Feat en LOCAL (tarea 6: -p / -pt). Es pesado: arrastra
# PyTorch. En este proyecto la tarea 6 se corrió en Colab (pyfeat_colab.ipynb),
# así que no hace falta instalarlo en local. Descomenta si vas a correr -p aquí.
# py-feat>=2.0.2
